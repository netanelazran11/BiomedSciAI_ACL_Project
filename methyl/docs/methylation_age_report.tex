\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{times}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{amsmath}

\begin{document}

\section{Introduction}
DNA methylation provides strong signals for age prediction, but the data are high-dimensional and noisy. We use a Transformer-based foundation model (MethylGPT) to learn sample-level representations from CpG tokens and establish a stable 8K-CpG baseline. This baseline serves as the reference point for evaluating a proposed BMFM-RNA architecture.
\begin{itemize}[leftmargin=1.5em]
\item \textbf{Article paper:} \href{https://www.biorxiv.org/content/10.1101/2024.10.30.621013v2}{\textbf{MethylGPT (bioRxiv 2024)}}
\item \href{https://github.com/netanelazran11/BiomedSciAI_ACL_Project}{\textbf{BiomedSciAI ACL Project (GitHub)}}
\end{itemize}

\section{Data Description}
\begin{table}[h]
\centering
\caption{Dataset summary and preprocessing.}
\label{tab:data}
\begin{tabular}{ll}
\toprule
Item & Description \\
\midrule
Pretraining sources & EWAS Data Hub; ClockBase \\
Collected profiles & 226,555 \\
After QC + deduplication & 154,063 \\
Platforms & Illumina 27K / 450K / EPIC \\
CpG harmonization & 49,156 sites ($\geq$5 EWAS traits and/or $\geq$95\% presence) \\
Value range & $\beta$-values in [0,1] \\
Missingness handling & Retained for masking-based objectives \\
\bottomrule
\end{tabular}
\end{table}

\section{Baseline Architecture}
\textbf{Pretraining data.} Large-scale methylation profiles (154,063 after QC) are used to learn general representations before downstream fine-tuning.

\begin{table}[h]
\centering
\caption{Architecture comparison (pretraining vs fine-tuning).}
\label{tab:arch_compare}
\begin{tabular}{lll}
\toprule
Component & Pretraining & Fine-tuning \\
\midrule
Base model & TransformerModel (scGPT) & Same TransformerModel (frozen/low LR) \\
Encoder & CpG embeddings & Same \\
Value encoder & ContinuousValueEncoder & Same \\
Transformer & 6 layers, 4 heads, $d=64$ & Same (loaded from pretrained) \\
Task head & ExprDecoder (MLM) + MVCDecoder & ResNet1D age head  \\
Output & Methylation reconstruction & Single age prediction \\
Dropout & 0.1 & 0.0 \\
Mask ratio & 30\% & 0\% \\
\bottomrule
\end{tabular}
\end{table}

This model fits our data because methylation profiles contain structured CpG dependencies that benefit from self-attention, while pretraining on large-scale methylation data learns generalizable token representations. The pretrained Transformer transfers to the 8K-CpG setting, and the ResNet1D head maps learned embeddings to age.

\textbf{Fine-tuning data.} We fine-tune on 11,453 labeled samples, each represented by 8,000 CpGs per sample (M=8000), with ages spanning 0--100 years and multi-tissue composition (blood $\sim$47.2\%, brain $\sim$34.5\%, others $\sim$18.3\%).

\textbf{Fine-tuning task.} Predict chronological age from the Transformer embeddings using a ResNet1D regression head (Conv1D 3x3, 6x ResBlock1D, AdaptiveAvgPool1d, Dropout, Linear, Sigmoid) with MSE loss. The output is a normalized age in [0,1]. The baseline results are reported in Section 7.

\textbf{Architecture flow (summary).} Pretraining applies MLM-style masked value prediction using the ExprDecoder; fine-tuning replaces the decoder with a ResNet1D age head that operates on Transformer outputs (excluding [CLS]) to produce a single normalized age prediction.

\section{Loss Function}
Pretraining uses masked MSE to reconstruct methylation values (MLM/MVP) with a 30\% mask ratio, along with a profile-level reconstruction objective. Fine-tuning uses MSE on normalized chronological age with no masking.

\section{Training Plan}
We first run a small hyperparameter sweep and select the best configuration using validation MAE only. Fine-tuning is initialized from pretrained weights. Training uses 100 epochs with an 80\% warmup phase and a final 20\% convergence phase. The best checkpoint is selected from epochs 81--100.

\section{Evaluation Plan}
We use a fixed 48/12/40 train/val/test split. Model selection uses validation MAE. The test set is used once per run for final evaluation. Generalization is assessed by repeating training with multiple random seeds (40--44). Metrics reported for age prediction include MAE, MedAE, RMSE, $R^2$, Pearson $r$, and Spearman $r$.

\section{Baseline Results}
\begin{table}[h]
\centering
\caption{MethylGPT baseline results (48/12/40 split, 5 seeds).}
\label{tab:results}
\begin{tabular}{lccc}
\toprule
Metric & Mean $\pm$ Std & Min & Max \\
\midrule
Validation MAE (years) & 5.12 $\pm$ 0.89 & 4.13 & -- \\
Test MAE (years) & 4.39 $\pm$ 0.18 & 4.13 & 4.62 \\
Test MedAE (years) & 3.06 $\pm$ 0.20 & -- & -- \\
Test $R^2$ & 0.941 $\pm$ 0.004 & -- & -- \\
Test Spearman & 0.958 $\pm$ 0.001 & -- & -- \\
\bottomrule
\end{tabular}
\end{table}

Test metrics are computed from the best checkpoint (selected by validation MAE). Validation MAE is averaged across all 100 training epochs.
For visual tracking and seed-level curves, see:
\begin{itemize}[leftmargin=1.5em]
\item \href{https://wandb.ai/netanelazran11-hebrew-university-of-jerusalem/methylGPT_8K_ElasticNet_Features/groups/ft8k_seeds_43622436_20260107_233307/workspace?nw=nwusernetanelazran11}{\textbf{MethylGPT W\&B Visual Results}}
\end{itemize}

\section{Proposed Changes: BMFM-RNA Architecture}

We adapt the BMFM-RNA SCBert architecture for methylation-based age prediction. The original BMFM-RNA is a BERT-based foundation model for single-cell RNA-seq ($\sim$110M parameters). We scale it down to match our 8K-CpG input.

\begin{itemize}[leftmargin=1.5em]
\item \textbf{Reference:} \href{https://research.ibm.com/projects/biomedical-foundation-models}{\textbf{BMFM-RNA (IBM Research Project)}}
\item \textbf{Code:} \href{https://github.com/netanelazran11/BiomedSciAI_ACL_Project}{\textbf{GitHub Repository}}
\end{itemize}

\subsection{Tokenizer Adaptation}

BMFM-RNA uses a \texttt{MultiFieldTokenizer} that handles multiple input fields (e.g., gene IDs and expression values). We adapt this for methylation by creating a custom tokenizer:

\textbf{Vocabulary construction.} We extract the 8{,}000 CpG site names (e.g., \texttt{cg00000029}, \texttt{cg00000108}) from the h5ad data file and build a vocabulary with 5 special tokens: \texttt{[UNK]}=0, \texttt{[SEP]}=1, \texttt{[PAD]}=2, \texttt{[CLS]}=3, \texttt{[MASK]}=4, followed by CpG site tokens (IDs 5--8004). Total vocab size: 8{,}005.

\textbf{Two-field input.} Each sample is represented as two parallel sequences:
\begin{enumerate}
\item \textbf{cpg\_sites}: Discrete token IDs $[3, s_1, s_2, \ldots, s_n, 2, 2, \ldots]$ where 3=[CLS], $s_i$ are CpG IDs, and 2=[PAD]
\item \textbf{beta\_values}: Continuous $\beta$-values $[0, \beta_1, \beta_2, \ldots, \beta_n, 0, 0, \ldots]$ in range $[0,1]$
\end{enumerate}

\textbf{Embedding combination.} The model combines both fields: $\mathbf{h}_i = \texttt{CpG\_embed}(s_i) + \texttt{Beta\_embed}(\beta_i) + \texttt{Pos\_embed}(i)$, where \texttt{Beta\_embed} is a learned MLP that projects continuous values to the hidden dimension.

\subsection{Architecture Comparison}

\begin{table}[h]
\centering
\caption{Architecture comparison: original BMFM-RNA, our adapted version, and MethylGPT.}
\label{tab:scbert_config}
\begin{tabular}{llll}
\toprule
Parameter & BMFM-RNA (original) & BMFM-RNA (ours) & MethylGPT (scGPT) \\
\midrule
Layers & 12 & 6 & 6 \\
Attention heads & 12 & 8 & 4 \\
Hidden size $d$ & 768 & 512 & 64 \\
FFN size & 3072 & 2048 & 256 \\
Head dimension & 64 & 64 & 16 \\
Parameters (encoder) & $\sim$110M & $\sim$23M & $\sim$2M \\
Vocab size & $\sim$20K (genes) & 8{,}005 & $\sim$8K \\
Max positions & varies & 8{,}002 & varies \\
CpG subset & -- & 8{,}000 & 8{,}000 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Multi-Field Tokenization}

Each CpG site $i$ is represented by two parallel fields combined via element-wise summation:
\begin{equation}
\mathbf{h}_i = \mathbf{e}_{\text{cpg}}(s_i) + \mathbf{e}_{\beta}(\beta_i)
\label{eq:embedding_sum}
\end{equation}
where $\mathbf{e}_{\text{cpg}}(s_i) \in \mathbb{R}^{512}$ is a learned CpG site embedding and $\mathbf{e}_{\beta}(\beta_i) \in \mathbb{R}^{512}$ is the output of a continuous value encoder. The sequence is passed through a 6-layer Transformer encoder.

\subsection{Fine-Tuning Pipeline}

The pretrained encoder (MLM loss = 0.0013, PCC = 0.997 after 240 epochs) is fine-tuned for age regression:
\begin{itemize}[leftmargin=1.5em]
\item \textbf{Pooling:} Mean pooling over token outputs (skip CLS)
\item \textbf{Age head:} MLP (512 $\to$ 256 $\to$ 128 $\to$ 1) with LayerNorm, GELU, Dropout(0.2)
\item \textbf{Freeze strategy:} Encoder frozen epochs 0--4, unfrozen epoch 5+ with 10$\times$ lower LR
\item \textbf{Loss:} MSE on $z$-score normalized ages
\end{itemize}

\subsection{Results}

\begin{table}[h]
\centering
\caption{BMFM-RNA fine-tuning results (8{,}000 CpG sites, dropout=0.2).}
\label{tab:bmfm_results}
\begin{tabular}{lcc}
\toprule
Split & MAE (years) & $R^2$ \\
\midrule
Train & 2.21 & -- \\
Validation & 5.12 & 0.917 \\
Test & 4.85 & 0.923 \\
\bottomrule
\end{tabular}
\end{table}

Training ran for 245 epochs (early stopping, patience=60). The model explains 92.3\% of age variance on the test set.

\section{Experimental Setup}

We use the same 8K-CpG dataset as the MethylGPT baseline (11{,}453 samples, train/valid/test split). Two baselines are evaluated on the same split: (1) mean prediction ($\hat{y}_i = \bar{y}_{\text{train}}$) and (2) Ridge regression with $\alpha$ selected by validation MAE (best: $\alpha=10$).

\begin{table}[h]
\centering
\caption{Training hyperparameters (MLP fine-tuning).}
\label{tab:training_config}
\begin{tabular}{ll}
\toprule
Hyperparameter & Value \\
\midrule
Optimizer & AdamW ($\beta_1{=}0.9$, $\beta_2{=}0.999$, $\epsilon{=}10^{-8}$) \\
Learning rate & $5 \times 10^{-4}$ \\
Weight decay & 0.01 \\
LR schedule & Cosine decay with 200-step linear warmup \\
Batch size & 32 (effective 64 with gradient accumulation $\times$2) \\
Max epochs & 300 \\
Precision & 16-bit mixed \\
Early stopping & Patience = 60 epochs on val/MAE \\
Checkpoint & Best validation MAE (top-3 saved) \\
\bottomrule
\end{tabular}
\end{table}

Experiment tracking: \href{https://wandb.ai/netanelazran11-hebrew-university-of-jerusalem/finetune-bmfm-rna-methylation-8k/runs/v4yn0u15/overview}{\textbf{W\&B run (bmfm-methyl-finetune-43998394)}}.

\section{Results}

\begin{table}[h]
\centering
\caption{Comparison across all approaches on the test set.}
\label{tab:all_comparison}
\begin{tabular}{lccc}
\toprule
Model & Test MAE (years) & Test $R^2$ & CpG sites \\
\midrule
Mean prediction & 22.82 & 0.00 & -- \\
Ridge ($\alpha = 10$) & 4.49 & 0.943 & 8{,}000 \\
MethylGPT baseline & 4.39 $\pm$ 0.18 & 0.941 $\pm$ 0.004 & 8{,}000 \\
\textbf{BMFM-RNA (ours)} & \textbf{4.85} & \textbf{0.923} & 8{,}000 \\
\bottomrule
\end{tabular}
\end{table}

Training ran for 245 epochs with early stopping (patience=60). The BMFM-RNA model achieves comparable performance to MethylGPT and Ridge regression using all 8{,}000 CpG sites. Train MAE = 2.21 years indicates moderate overfitting (train--test gap of $\sim$2.6 years), reduced from earlier experiments by increasing dropout to 0.2.

\section{Discussion and Conclusions}

\subsection{Why BMFM-RNA Architecture for Methylation?}

The BMFM-RNA SCBert architecture offers several advantages for methylation-based age prediction:

\textbf{Multi-field tokenization.} Unlike standard approaches that treat methylation values as a flat vector, BMFM uses a dual-field representation: each CpG site is encoded as $\mathbf{h}_i = \mathbf{e}_{\text{cpg}}(s_i) + \mathbf{e}_{\beta}(\beta_i)$. The CpG ID embedding captures site-specific information (genomic context, regulatory role), while the continuous value encoder captures the methylation state. This separation allows the model to learn which sites are informative independently of their current methylation level.

\textbf{Continuous value encoding.} The $\beta$-value encoder uses an MLP to project continuous methylation values into the embedding space, rather than discretizing them into bins. This preserves fine-grained information that may be lost in quantization-based approaches.

\textbf{Self-attention over CpG sites.} The Transformer encoder learns pairwise relationships between CpG sites. Methylation patterns are known to be correlated across genomic regions (e.g., CpG islands, promoters). Self-attention can capture these long-range dependencies without explicit feature engineering.

\subsection{Why Pretraining Helps}

\textbf{Transfer learning from large-scale data.} The encoder is pretrained on 154{,}063 methylation profiles using masked value prediction (MLM). During pretraining, the model learns to reconstruct masked $\beta$-values from context, forcing it to understand the statistical structure of methylation patterns across diverse tissues and conditions. These learned representations transfer to the age prediction task.

\textbf{Pretrained components.} The checkpoint provides well-trained: (1) CpG site embeddings that encode site-specific properties, (2) $\beta$-value encoder that maps continuous values to meaningful representations, (3) Transformer layers that capture inter-site dependencies, and (4) position embeddings. Without pretraining, the model must learn all of this from the smaller fine-tuning dataset (11{,}453 samples), which is insufficient for a 23M parameter model.

\textbf{Pretraining loss as quality indicator.} Our pretrained checkpoint achieved MLM loss = 0.0013 and PCC = 0.997 after 240 epochs, indicating the encoder accurately reconstructs methylation patterns. This strong reconstruction ability suggests the learned representations capture biologically meaningful information.

\subsection{Key Implementation Changes}

Several critical modifications were required to achieve good performance:

\textbf{Freeze-then-unfreeze strategy.} The encoder is frozen for the first 5 epochs while the age head learns to map pretrained representations to age. At epoch 5, the encoder is unfrozen with 10$\times$ lower learning rate. This prevents catastrophic forgetting of pretrained knowledge while allowing task-specific adaptation.

\textbf{Optimizer configuration fix.} A critical bug was identified: PyTorch Lightning's \texttt{configure\_optimizers()} is called once at initialization. If encoder parameters are frozen (requires\_grad=False) at init, they are excluded from the optimizer. Simply setting requires\_grad=True later does not add them to the optimizer. The fix is to include all parameters in the optimizer from the start---frozen parameters receive no gradients and are skipped automatically.

\textbf{Mean pooling instead of CLS.} MLM pretraining does not train the [CLS] token to aggregate sequence information (unlike NSP in BERT). We use mean pooling over content tokens, which empirically works better for regression tasks after MLM pretraining.

\textbf{Z-score normalization.} Ages are normalized to zero mean and unit variance during training. This stabilizes gradients and improves convergence, especially important when the target range (0--100 years) differs significantly from typical neural network output scales.

\subsection{Results Analysis}

\textbf{BMFM-RNA performance.} Using the corrected pipeline with all 8{,}000 CpG sites and dropout=0.2, the model achieves Test MAE = 4.85 years and $R^2$ = 0.923. This is within 8\% of Ridge regression (4.49 years) and 10\% of MethylGPT (4.39 years).

\textbf{Effect of using all CpGs.} Increasing from 2{,}048 to 8{,}000 CpG sites provided only marginal improvement (4.87 $\to$ 4.85 years). This suggests the fixed 2{,}048 CpG subset already captured most of the age-predictive signal, and the Transformer encoder effectively learns from a compressed input.

\textbf{Overfitting reduction.} Increasing dropout from 0.1 to 0.2 reduced the train--test MAE gap from $\sim$2.9 years to $\sim$2.6 years (train MAE = 2.21, test MAE = 4.85). Further regularization or data augmentation may continue to narrow this gap.

\subsection{Limitations and Future Work}

\begin{enumerate}
\item \textbf{Single training run.} Results are from one seed; multi-seed evaluation is needed for robust confidence intervals.
\item \textbf{Performance gap.} BMFM-RNA (4.85) is still 8\% behind Ridge (4.49) despite using a 23M parameter Transformer. This may indicate that the pretrained representations are not fully leveraged, or that methylation age prediction is fundamentally a linear problem.
\item \textbf{Architecture search.} The current head architecture (512$\to$256$\to$128$\to$1) was not extensively tuned. Simpler or deeper heads may perform better.
\item \textbf{Tissue-specific models.} The current model is trained on mixed tissues. Tissue-specific fine-tuning may reduce prediction error.
\end{enumerate}

\section{References}
\begin{enumerate}[leftmargin=1.5em]
\item Ying, K., et al., ``MethylGPT: a foundation model for the DNA methylome,'' \emph{bioRxiv}, 2024.
\item Li, D., et al., ``BMFM-RNA: A biomedical foundation model for single-cell transcriptomics,'' IBM Research, 2024.
\item Horvath, S., ``DNA methylation age of human tissues and cell types,'' \emph{Genome Biology}, 14(10):R115, 2013.
\item Hannum, G., et al., ``Genome-wide methylation profiles reveal quantitative views of human aging rates,'' \emph{Molecular Cell}, 49(2):359--367, 2013.
\item de Lima Camillo, L.P., et al., ``AltumAge: A pan-tissue DNA methylation epigenetic clock based on deep learning,'' \emph{npj Aging}, 8(1):1--15, 2022.
\end{enumerate}

\end{document}
